import { useState, useEffect, useRef } from "react";
import { ConsumptionsService } from "../../../services/consumptions.service";

function Graph(){
  const [consumption, setConsumption] = useState({timeArray:[], electricityDemandArray:[]});
  return (
    <>
    </> 
  )
};

export default Graph;

